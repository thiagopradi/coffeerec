<div class="space-y-6">
  <h1 class="text-3xl font-bold text-white">Dashboard</h1>

  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <!-- Users Card -->
    <div class="bg-slate-800 rounded-xl p-6 border border-slate-700">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-slate-400 text-sm">Total Users</p>
          <p class="text-3xl font-bold text-white mt-1"><%= @users_count %></p>
        </div>
        <div class="w-12 h-12 bg-blue-500/20 rounded-lg flex items-center justify-center text-2xl">
          👥
        </div>
      </div>
      <%= link_to "Manage users →", admin_users_path, class: "inline-block mt-4 text-blue-400 hover:text-blue-300 text-sm" %>
    </div>

    <!-- Coffees Card -->
    <div class="bg-slate-800 rounded-xl p-6 border border-slate-700">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-slate-400 text-sm">Total Coffees</p>
          <p class="text-3xl font-bold text-white mt-1"><%= @coffees_count %></p>
        </div>
        <div class="w-12 h-12 bg-amber-500/20 rounded-lg flex items-center justify-center text-2xl">
          ☕
        </div>
      </div>
      <%= link_to "Manage coffees →", admin_coffees_path, class: "inline-block mt-4 text-amber-400 hover:text-amber-300 text-sm" %>
    </div>

    <!-- Profiles Card -->
    <div class="bg-slate-800 rounded-xl p-6 border border-slate-700">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-slate-400 text-sm">Taste Profiles</p>
          <p class="text-3xl font-bold text-white mt-1"><%= @profiles_count %></p>
        </div>
        <div class="w-12 h-12 bg-green-500/20 rounded-lg flex items-center justify-center text-2xl">
          🎯
        </div>
      </div>
      <%= link_to "View matches →", admin_matches_path, class: "inline-block mt-4 text-green-400 hover:text-green-300 text-sm" %>
    </div>
  </div>

  <!-- Quick Actions -->
  <div class="bg-slate-800 rounded-xl p-6 border border-slate-700">
    <h2 class="text-xl font-semibold text-white mb-4">Quick Actions</h2>
    <div class="flex gap-4">
      <%= link_to "Add User", new_admin_user_path, class: "px-4 py-2 bg-blue-600 hover:bg-blue-500 text-white rounded-lg transition" %>
      <%= link_to "Add Coffee", new_admin_coffee_path, class: "px-4 py-2 bg-amber-600 hover:bg-amber-500 text-white rounded-lg transition" %>
    </div>
  </div>
</div>
