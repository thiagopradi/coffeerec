<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin - Coffee Sommelier" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-sans antialiased bg-slate-900 text-slate-100 min-h-screen">
    <div class="flex min-h-screen">
      <!-- Sidebar -->
      <aside class="w-64 bg-slate-800 border-r border-slate-700 flex flex-col">
        <div class="p-6 border-b border-slate-700">
          <h1 class="text-xl font-bold text-amber-400">☕ Admin Panel</h1>
        </div>
        <nav class="flex-1 p-4">
          <ul class="space-y-2">
            <li>
              <%= link_to admin_root_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition #{request.path == admin_root_path ? 'bg-amber-500/20 text-amber-400' : 'text-slate-300 hover:bg-slate-700'}" do %>
                <span>📊</span>
                <span>Dashboard</span>
              <% end %>
            </li>
            <li>
              <%= link_to admin_users_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition #{request.path.start_with?('/admin/users') ? 'bg-amber-500/20 text-amber-400' : 'text-slate-300 hover:bg-slate-700'}" do %>
                <span>👥</span>
                <span>Users</span>
              <% end %>
            </li>
            <li>
              <%= link_to admin_coffees_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition #{request.path.start_with?('/admin/coffees') ? 'bg-amber-500/20 text-amber-400' : 'text-slate-300 hover:bg-slate-700'}" do %>
                <span>☕</span>
                <span>Coffees</span>
              <% end %>
            </li>
            <li>
              <%= link_to admin_matches_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition #{request.path.start_with?('/admin/matches') ? 'bg-amber-500/20 text-amber-400' : 'text-slate-300 hover:bg-slate-700'}" do %>
                <span>🎯</span>
                <span>Matches</span>
              <% end %>
            </li>
          </ul>
        </nav>
        <div class="p-4 border-t border-slate-700">
          <%= link_to root_path, class: "flex items-center gap-3 px-4 py-2 text-slate-400 hover:text-slate-200 transition" do %>
            <span>←</span>
            <span>Back to site</span>
          <% end %>
        </div>
      </aside>

      <!-- Main content -->
      <main class="flex-1 flex flex-col">
        <% if flash[:alert] %>
          <div class="mx-6 mt-4 px-4 py-3 bg-red-500/20 border border-red-500/50 text-red-300 rounded-lg">
            <%= flash[:alert] %>
          </div>
        <% end %>

        <% if flash[:notice] %>
          <div class="mx-6 mt-4 px-4 py-3 bg-green-500/20 border border-green-500/50 text-green-300 rounded-lg">
            <%= flash[:notice] %>
          </div>
        <% end %>

        <div class="flex-1 p-6">
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
